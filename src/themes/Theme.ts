'use client';

import { createTheme } from '@mui/material/styles';

/**
 *  Theme Einstellungen von MUI.
 *
 *  @author Ole Nordhusen
 *  @version 1.0.0
 *  @since 1.0.0
 */
const theme = createTheme({
  typography: {
    fontFamily: 'var(--font-roboto)',
  },
});

export default theme;